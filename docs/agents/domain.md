# Domain documentation

This is a single-context repository. Before changing a feature, read the root
`CONTEXT.md` and the ADRs in `docs/adr/` that apply to the affected area.

Use the terms defined in `CONTEXT.md` consistently in issues, tests, and
documentation. Surface an ADR conflict explicitly rather than silently
overriding it.
